#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,c,d,e;
    scanf("%d %d\n%d",&a,&b,&c);
    d=(b+c)%60;
    e=(b+c)/60;
    a+=e;
    if(a>=24)
    {
        a-=24;
    }
    printf("%d %d",a,d);
    return 0;
}
