#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,c=0,d=0,i;
    for(i=0;i<10;i++)
    {
        scanf("%d %d",&a,&b);
        c-=a;
        c+=b;
        if(c>d)
        {
            d=c;
        }
    }
    printf("%d",d);
    return 0;
}
